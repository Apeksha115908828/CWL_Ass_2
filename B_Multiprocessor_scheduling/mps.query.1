task(1, 3).  % Task ID 1 with length 3
task(2, 2).  % Task ID 2 with length 2
task(3, 2).  % Task ID 3 with length 1

% Parameters
#const m = 2.
#const d = 5.
%x(0..m), .
%y(0..d).

start(T, x, y, L) :- task(T, L), L < d, -(d, L, d1), -(m, 1, m1), x(0..m1), y(0..d1).
:- start(T1, P, S1, _), start(T2, P, S2, _), T1 != T2, S1 == S2.
:- start(T1, P, S1, L1), start(T2, P, S2, L2), S1 > S2, +(S2, L2, E2), S1 < E2.

start(T, P, S, L)?
